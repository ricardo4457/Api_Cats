class Cat {
   constructor(id, tags, mimetype, createdAt) {
      this.id = id;             
      this.tags = tags;         
      this.mimetype = mimetype; 
      this.createdAt = createdAt; 
    }
}

module.exports = Cat;
